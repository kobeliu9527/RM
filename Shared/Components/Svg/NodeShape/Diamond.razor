<path stroke="@Node.AppearanceInfo.BorderColor"
      width="@Node.AppearanceInfo.Width"
      height="@Node.AppearanceInfo.Height"
      d="m0,@(Node.AppearanceInfo.Height/2)l@(Node.AppearanceInfo.Width/2),-@(Node.AppearanceInfo.Height/2)l@(Node.AppearanceInfo.Width/2),@(Node.AppearanceInfo.Height/2)l-@(Node.AppearanceInfo.Width/2),@(Node.AppearanceInfo.Height/2)l-@(Node.AppearanceInfo.Width/2),-@(Node.AppearanceInfo.Height/2)z"
      fill="#fff" />
<text y="@(Node.AppearanceInfo.Height/2)" x="@(Node.AppearanceInfo.Width/2)"
      width="@Node.AppearanceInfo.Width" height="@Node.AppearanceInfo.Width"
      style="text-anchor:middle">
    <tspan font-weight="bold" fill="@Node.AppearanceInfo.TextColor">@Node.AppearanceInfo.NodeModelName</tspan>
</text>
@if (Node != null)
{
 @*    @if (Node.GetPort(PortAlignment.TopLeft) != null)
    {
        <PortRenderer Port="Node.GetPort(PortAlignment.TopLeft)">
            <circle class="port" cx="10" cy="10" r="10"></circle>
        </PortRenderer>
    } *@
    @if (Node.GetPort(PortAlignment.Top) != null)
    {
        <PortRenderer Port="Node.GetPort(PortAlignment.Top)">
            <circle class="port" cx="@(Node.AppearanceInfo.Width/2)" cy="10" r="10"></circle>
        </PortRenderer>
    }
@*     @if (Node.GetPort(PortAlignment.TopRight) != null)
    {
        <PortRenderer Port="Node.GetPort(PortAlignment.TopRight)">
            <circle class="port" cx="80" cy="10" r="10"></circle>
        </PortRenderer>
    } *@
 @*    @if (Node.GetPort(PortAlignment.BottomLeft) != null)
    {
        <PortRenderer Port="Node.GetPort(PortAlignment.BottomLeft)">
            <circle class="port" cx="10" cy="80" r="10"></circle>
        </PortRenderer>
    } *@
    @if (Node.GetPort(PortAlignment.Bottom) != null)
    {
        var xx = Node.AppearanceInfo;
        ;
        <PortRenderer Port="Node.GetPort(PortAlignment.Bottom)">
            <circle class="port" cx="@(Node.AppearanceInfo.Width/2)" cy="@(Node.AppearanceInfo.Height-10)" r="10"></circle>
        </PortRenderer>
    }
@*     @if (Node.GetPort(PortAlignment.BottomRight) != null)
    {
        <PortRenderer Port="Node.GetPort(PortAlignment.BottomRight)">
            <circle class="port" cx="80" cy="80" r="10"></circle>
        </PortRenderer>
    } *@
    @if (Node.GetPort(PortAlignment.Left) != null)
    {
        <PortRenderer Port="Node.GetPort(PortAlignment.Left)">
            <circle class="port" cx="10" cy="@(Node.AppearanceInfo.Height/2)" r="10"></circle>
        </PortRenderer>
    }
    @if (Node.GetPort(PortAlignment.Right) != null)
    {
        <PortRenderer Port="Node.GetPort(PortAlignment.Right)">
            <circle class="port" cx="@(Node.AppearanceInfo.Width-10)" cy="@(Node.AppearanceInfo.Height/2)" r="10"></circle>
        </PortRenderer>
    }

}
@code {
    [Parameter]
    [NotNull]
    public CustomNode? Node { get; set; }
}
