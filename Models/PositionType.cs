namespace Models
{
    /// <summary>
    /// html元素的定位方式
    /// </summary>
    public enum PositionType
    {
        Static,
        Relative,
        Absolute,
        Fixex,
        Sticky
    }
}
